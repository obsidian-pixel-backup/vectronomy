/**
 * VECTRONOMY — Interactive SVG Editor (v2)
 *
 * Handles: selection, move, resize, draw (rect/circle/line/pen),
 * fill/stroke editing, opacity, rotation, z-order, align, undo/redo.
 */
import type { ConvertedLayer } from './types';
import { RasterEngine } from './rasterEngine';
import { BooleanOp } from './pathfinder';
import { NestingOptions } from './nesting';
export interface ElementProperties {
    x: number;
    y: number;
    w: number;
    h: number;
    rotation: number;
    opacity: number;
    strokeW: number;
    strokeColor: string;
    strokeCap: string;
    strokeJoin: string;
    strokeOpacity: number;
    fillEnabled: boolean;
    fillColor: string;
    fillOpacity: number;
    fillRule: string;
    elementType: string;
    textContent?: string;
    fontFamily?: string;
    fontSize?: number;
    fontWeight?: string;
    fontStyle?: string;
    letterSpacing?: number;
    wordSpacing?: number;
    textAnchor?: string;
}
export interface PenPoint {
    x: number;
    y: number;
    cp1x?: number;
    cp1y?: number;
    cp2x?: number;
    cp2y?: number;
}
export declare class VectorEditor {
    private onDrawingChange?;
    private container;
    private currentLayer;
    selectedId: string | null;
    selectedIds: Set<string>;
    isMarquee: boolean;
    marqueeStart: DOMPoint | null;
    marqueeEl: SVGRectElement | null;
    private selectionGroup;
    private onUpdate;
    private onSelectionChange;
    isolatedGroupId: string | null;
    enterIsolation(groupId: string): void;
    exitIsolation(): void;
    private onInteractionStart;
    private onInteractionEnd;
    private isDragging;
    private dragMode;
    private dragPivotX;
    private dragPivotY;
    private accumulatedRotation;
    private lastAppliedRotation;
    private activeHandle;
    private dragStartX;
    private dragStartY;
    private origBBox;
    selectedNodeIndices: Set<number>;
    private parsedCommands;
    private editingNodes;
    private draggingNode;
    private nodeEditTarget;
    private lastSnappedPt;
    activeTool: 'select' | 'pan' | 'rect' | 'circle' | 'line' | 'pen' | 'node' | 'polygon' | 'star' | 'spiral' | 'pencil' | 'polyline' | 'brush' | 'eraser' | 'magic-wand' | 'frame' | 'scissors' | 'text';
    private isDrawing;
    private drawingEl;
    private drawStartX;
    private drawStartY;
    private penPoints;
    private penDraggingPoint;
    rasterEngine: RasterEngine;
    handleSnapEnabled: boolean;
    polygonSides: number;
    starPoints: number;
    private pencilPoints;
    private polylinePoints;
    brushSize: number;
    brushStyle: 'round' | 'calligraphic' | 'flat';
    currentStrokeColor: string;
    private magicWandThreshold;
    private brushPoints;
    private eraserPoints;
    private brushCursor;
    private snapFn;
    setSnapFunction(fn: ((pt: {
        x: number;
        y: number;
    }) => {
        x: number;
        y: number;
    }) | null): void;
    centroidSnapEnabled: boolean;
    transformPivot: 'tl' | 'tc' | 'tr' | 'cl' | 'cc' | 'cr' | 'bl' | 'bc' | 'br';
    constructor(container: HTMLElement, onUpdate: (svg: string) => void, onSelectionChange: (props: ElementProperties | null) => void, onInteractionStart: () => void, onInteractionEnd: () => void, onDrawingChange?: (() => void) | undefined);
    shiftPressed: boolean;
    altPressed: boolean;
    private boundOnMouseMove;
    private boundOnMouseUp;
    private boundOnKeyDown;
    private boundOnKeyUp;
    private boundOnMouseDown;
    private boundOnDblClick;
    private boundOnMouseLeave;
    private init;
    destroy(): void;
    private onMouseLeave;
    private updateBrushCursor;
    private onKeyDown;
    private onKeyUp;
    getContainer(): HTMLElement;
    selectAll(): void;
    setLayer(layer: ConvertedLayer): void;
    setTool(tool: typeof this.activeTool): void;
    pathfinderDivide(): Promise<void>;
    pathfinderOperation(operation: BooleanOp): Promise<void>;
    togglePathClosed(): void;
    strokeToPath(): void;
    offsetSelectedPaths(offset: number): void;
    flattenSelectedPaths(tolerance: number): void;
    reverseSelectedPaths(): void;
    private processPathfinderUtility;
    applyKerf(kerfWidth: number, direction?: 'auto' | 'inward' | 'outward'): void;
    applyLeadInOut(length: number, type: 'in' | 'out' | 'both'): void;
    applyMicroJoints(count: number, width: number): void;
    applyAcrylicOvercut(length: number): void;
    applyPowerRamping(thresholdDeg?: number): void;
    applyPerforations(dashLen: number, gapLen: number): void;
    mapColorsToOps(mapping: {
        color: string;
        mode: string;
    }[]): void;
    applyMultiPass(passes: number): void;
    optimizeCutOrder(): Promise<void>;
    generate3DAssembly(containerId: string, thickness: number, materialType?: 'wood' | 'acrylic'): Promise<void>;
    applyNesting(options: NestingOptions): void;
    applyCncBitOffset(bitRadius: number): void;
    applyPocketHatch(stepover: number, angleDeg: number): void;
    applyChamfer(depth: number, angleDeg: number, passes: number): void;
    convertToDrill(maxDiameter: number): void;
    applyVCarve(bitAngle: number): void;
    applyMillingHoldingTabs(tabCount: number, tabWidth: number, tabThickness: number): void;
    applyConcentricHatch(stepover: number): void;
    applyFeedPlungeRates(feedXY: number, plungeZ: number): void;
    applyStepoverOverlapConfig(overlapPercent: number): void;
    applySafeRetractZ(retractZ: number): void;
    traceCenterline(cornerThreshold?: number): Promise<void>;
    traceSilhouette(threshold: number, cornerThreshold?: number): Promise<void>;
    tracePosterized(colors: number, cornerThreshold?: number): Promise<void>;
    traceEdges(threshold: number, cornerThreshold?: number): Promise<void>;
    traceColorIsolation(colorHex: string, tolerance?: number, cornerThreshold?: number): Promise<void>;
    applyHalftone(spacing: number, angle: number): Promise<void>;
    applyDespeckle(minArea: number): void;
    private replaceImageWithVectors;
    joinOpenEndpoints(threshold?: number): void;
    applyImageFilters(brightness: number, contrast: number, blur: number): void;
    createFrameFromSelection(): void;
    convertTextToPath(textEl?: SVGTextElement): Promise<SVGGElement | undefined>;
    warpTextToPath(textEl?: SVGTextElement, targetPath?: SVGPathElement): Promise<void>;
    injectStencilBridges(bridgeWidth?: number): void;
    private onMouseDown;
    convertSelectedToPath(): Promise<void>;
    convertToPath(el: SVGElement): SVGPathElement;
    private eraserModified;
    private onMouseMove;
    private centroidGuideH;
    private centroidGuideV;
    private clearCentroidGuides;
    private drawCentroidGuides;
    private onMouseUp;
    private addNodeAtCursor;
    private onDblClick;
    private startDraw;
    private updateDraw;
    private finalizeDraw;
    private eraserCache;
    private performVectorEraser;
    private getPointsFromD;
    private getCalligraphicD;
    finalizePolyline(): void;
    private getSmoothedPencilD;
    private simplifyPoints;
    private getSqSegDist;
    createGridArray(rows: number, cols: number, spacingX: number, spacingY: number): void;
    canUndoDrawing(): boolean;
    undoDrawing(): void;
    cancelDrawing(): void;
    private finalizePenPath;
    private renderPenOverlay;
    private getPathD;
    private selectElement;
    private getScale;
    getSvgPoint(e: MouseEvent | Touch): DOMPoint | null;
    getSelectedEls(): SVGGraphicsElement[];
    private getUnionBBox;
    clearSelection(): void;
    private deleteActiveNode;
    deleteSelected(): void;
    private getSelectedEl;
    renderSelectionUI(): void;
    private renderNodeUI;
    private getCursorForHandle;
    private getTransformedBBox;
    private getBBoxInViewport;
    private translateEl;
    private resizeSelection;
    private rotateSelection;
    private resizeEl;
    private getPivotCoords;
    private buildElementProperties;
    getElementProperties(id: string | null): ElementProperties | null;
    getMultiSelectionProperties(): ElementProperties | null;
    private notifyChange;
    updateProperties(props: Partial<ElementProperties>): void;
    bringToFront(): void;
    bringForward(): void;
    sendBackward(): void;
    sendToBack(): void;
    nudgeSelected(dx: number, dy: number): void;
    alignTo(mode: 'left' | 'center-h' | 'right' | 'top' | 'center-v' | 'bottom'): void;
    alignSelectedNodes(alignment: 'left' | 'center-h' | 'right' | 'top' | 'center-v' | 'bottom'): void;
    cutSegmentAtCursor(e: MouseEvent, el: SVGPathElement): void;
    commit(): void;
    insertImage(dataUrl: string, x: number, y: number, width: number, height: number): void;
    mergeSelectedLayers(): void;
    createSymbol(targetElement?: SVGGraphicsElement): void;
    exportSelectionToSvg(): void;
}
//# sourceMappingURL=editor.d.ts.map